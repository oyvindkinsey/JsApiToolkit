/* ---------------------------------------* 
 * Export the library to the global scope *
 * ---------------------------------------*/
this[library.name] = library;
}(window, document /*, undefined*/));